<html>
<head>
    <link rel="stylesheet" href="/home/css/style.css">
    <link rel="stylesheet" type="text/css" href="/navbar/css/style.css">
    <script src="/home/js/login.js"></script>
</head>
</head>
<body>
    <%- include('navbar.ejs') %>
    </div>
    <div id="container">
        <h2>Genre</h2>
        <div id="genres">
            <div class="sectionContainer">
                <% genres.forEach((genre) =>{ %>
                    <a class="inner" href="/genre/<%=genre._id%>"> 
                        <h1 class="title"><%=genre._id%></h1>
                        <img class="genreImage" src="<%=genre.url%>"></img>
                    </a>
                <% }); %>
            </div>
        </div>
        <h2>Popular Books</h2>
        <div id="popularBooks" >
            <div class="sectionContainer">
            <% popularBooks.forEach((book) =>{ %>
                <a class="inner" href="/book/<%=book._id%>"> 
                    <h1 class="title"><%=book._id%></h1>
                    <img class="bookImage" src="<%=book.cover%>"></img>
                </a>
            <% }); %>
            </div>
        </div>
        <h2>Popular Authors</h2>
        <div id="popularAuthors">
            <div class="sectionContainer">
            <% authors.forEach((author) =>{ %>
                <a class="inner" href="/author/<%=author._id%>"> 
                    <h1 class="title"><%=author._id%></h1>
                    <img class="authorImage" src="<%=author.picture%>"></img>
                </a>
            <% }); %>
            </div>
        </div>
    </div>
    <aside>
        <div>Frequently asked questions</div>
    </aside>
<%- include('footer.ejs') %>
